# Auto-generated for client: elangotest
apiVersion: v1
kind: ServiceAccount
metadata:
  name: aggregator-sa-elangotest
  namespace: aggregates
  annotations:
    iam.gke.io/gcp-service-account: "elangotest-kong-aggregator@sherlock-004.iam.gserviceaccount.com"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: aggregator-elangotest
  namespace: aggregates
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aggregator-elangotest
  template:
    metadata:
      labels:
        app: aggregator-elangotest
    spec:
      serviceAccountName: aggregator-sa-elangotest
      containers:
      - name: aggregator
        image: "us-central1-docker.pkg.dev/sherlock-004/ts43/aggregates:v1.0.1"
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: aggregator-config-elangotest
        volumeMounts:
        - name: gcp-key-volume
          mountPath: "/gcp"
          readOnly: true
        - name: pricing-volume
          mountPath: "/config"
          readOnly: true
      volumes:
      - name: gcp-key-volume
        secret:
          secretName: key
      - name: pricing-volume
        configMap:
          name: pricing-config-elangotest
---
apiVersion: v1
kind: Service
metadata:
  name: log-sink-elangotest
  namespace: aggregates
spec:
  selector:
    app: aggregator-elangotest
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: bq-aggregator-trigger-elangotest
  namespace: aggregates
spec:
  schedule: "0 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: trigger-curl
            image: curlimages/curl:7.82.0
            args:
            - "-X"
            - "POST"
            - "http://log-sink-elangotest.aggregates.svc.cluster.local:8080/trigger_aggregation"
          restartPolicy: OnFailure
