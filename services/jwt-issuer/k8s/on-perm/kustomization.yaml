apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kong

resources:
  - ../base

# Pin/override your image/tag at deploy-time
images:
  - name: us-central1-docker.pkg.dev/sherlock-004/ts43/jwt-issuer
    newTag: v5  # change to a git SHA/tag you build

patches:
  - path: patch-env.yaml
    target:
      kind: Deployment
      name: jwt-issuer
